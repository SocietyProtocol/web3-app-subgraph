type User @entity(immutable: false) {
  id: ID!
  profile: Badge
  badges: [Badge!]!
}

type Badge @entity(immutable: false) {
  id: ID!
  name: String!
  isOfficial: Boolean!
  uri: String
  hookAddress: Bytes!
  owners: [User!]! @derivedFrom(field: "badges")
  createdAt: BigInt!
}
