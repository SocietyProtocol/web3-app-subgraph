{
  "name": "society-protocol-badges-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "prepare:mainnet": "node scripts/prepare.js mainnet",
    "prepare:sepolia": "node scripts/prepare.js sepolia",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "yarn run prepare:mainnet && graph codegen && graph build && graph deploy --node https://api.studio.thegraph.com/deploy/ society-mainnet",
    "deploy:testnet": "yarn run prepare:sepolia && graph codegen && graph build && graph deploy --node https://api.studio.thegraph.com/deploy/ society-testnet",
    "create-local": "graph create --node http://localhost:8020/ society-mainnet",
    "remove-local": "graph remove --node http://localhost:8020/ society-mainnet",
    "deploy-local": "yarn run prepare:mainnet && graph codegen && graph build && graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 society-mainnet",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.35.1"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.2"
  }
}
